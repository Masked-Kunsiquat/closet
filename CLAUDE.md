# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**hangr** — a personal, local-first digital wardrobe and outfit tracking app. Native Android, Kotlin, Jetpack Compose, Room/SQLite. No accounts, no cloud, no telemetry.

All app code lives in `app-android/`. Run all commands from that directory. See `app-android/CLAUDE.md` for full project guidance.